function drawEnding3() {
  background(22, 22, 70);

  // Title
  fill(255);
  textAlign(CENTER);
  textSize(52);
  text("Wavelength, Shannon Edition!", width / 2, 140);

  // Subtitle
  fill(0);
  textSize(16);
  text(
    "Yeah no you've lost the plot fs, pinapple on pizza is goated broski",
    width / 2,
    185
  );
}
