function drawEnding2() {
  background(222);

  // Title
  fill(255);
  textAlign(CENTER);
  textSize(52);
  text("Wavelength, Shannon Edition!", width / 2, 140);

  // Subtitle
  fill(0);
  textSize(16);
  text(
    "Hell yeah, we’re on the same wavelength!!!!!\nI was born and raised in Barbados and so I absolutely love Caribbean music too!!",
    width / 2,
    185
  );
}
