function drawEnding6() {
  background(200, 100, 255);

  // Title
  fill(255);
  textAlign(CENTER);
  textSize(52);
  text("Not quite.", width / 2, 100);

  textSize(30);
  text("Barbados became independent on Nov. 20th 1966", width / 2, 140);

  // Subtitle
  fill(0);
  textSize(16);
  text(
    "You got 1 out of 2 questions though! Thanks for playing!",
    width / 2,
    185
  );

  //drawButton(end5);
  // drawButton(end6);
}
