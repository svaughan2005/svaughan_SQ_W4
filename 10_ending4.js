function drawEnding4() {
  background(22, 22, 70);

  // Title
  fill(255);
  textAlign(CENTER);
  textSize(52);
  text("Wavelength, Shannon Edition!", width / 2, 140);

  // Subtitle
  fill(0);
  textSize(16);
  text(
    "Hell yeah!! Yk I was a lil worried originally that you didn't catch my BALANCE reference but\n you really redeemed yourself by liking pineapple on pizza.",
    width / 2,
    185
  );
}
